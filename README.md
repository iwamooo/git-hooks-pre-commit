# Git hooks pre-commit(husky) - ESLint
## Recommended Env
- node: `^10.15.1`
- npm: `^6.9.0`

## Installation
```
$ npm i
```

## Usage
```
$ echo "NG" >> commit.text
$ git add commit.text
$ git commit -m "[add], commit.text"
```
